$kind: http-request
name: Get or Create Conversation
method: GET
url: '{{baseUrl}}/api/messages/:userId'
order: 2000
description: Gets an existing private conversation with the specified user, or creates a new one if none exists. Returns the conversationId.
pathVariables:
  - key: userId
    value: '123'
